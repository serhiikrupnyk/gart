{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["node", "jest"]
  },
  // Only the specs. Everything they import from src is still checked, but src is
  // not listed here so it does not get jest globals in scope.
  "include": ["test/**/*"]
}
